# site

Public catalog mockup (Vue 3 + Vite).

Live: https://ercastil.github.io/rodal3d-catalog/

```bash
cd site
npm install
npm run dev
```

GitHub Pages is built from this folder by `.github/workflows/pages.yml` on every push to `main`.
